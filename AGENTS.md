# kigumi-angular

Angular starter project using Kigumi CLI and Web Awesome web components.

## Tech Stack

- Angular 21, TypeScript, Web Awesome
- Kigumi CLI for component generation and theming
- Angular ESLint for code quality

## Project Structure

- `src/components/ui/` -- **Generated by Kigumi CLI. Do not edit manually.**
- `src/components/examples/` -- Example screens (LoginExample)
- `src/lib/kigumi.ts` -- **Generated.** Web Awesome setup and theme classes
- `src/styles/layers.css` -- **Generated.** CSS cascade layer definitions
- `src/styles/theme.css` -- **Generated.** Custom theme overrides (editable via `npx kigumi theme`)
- `src/app/app.ts` -- Application root
- `src/main.ts` -- Entry point (imports kigumi.ts before bootstrap)

## Key Rules

1. **Never edit generated files directly.** Use Kigumi CLI commands instead:
   - `npx kigumi add <component>` -- add a component
   - `npx kigumi update` -- update components to latest templates
   - `npx kigumi theme <name>` -- change theme
   - `npx kigumi brand <color>` -- change brand color
2. **`CUSTOM_ELEMENTS_SCHEMA`** is required in any component that uses `<wa-*>` elements directly. Kigumi wrappers (`<k-*>`) already include this.
3. **CSS cascade layers** (base < theme) handle specificity. Never use `!important`.
4. **`--wa-*` CSS custom properties** are the token layer for theming.

## Component Pattern (Angular)

Wrappers are standalone components with `@ViewChild('element')` for DOM ref access. Props use `@Input()`, events use `@Output()` with `EventEmitter`. Event listeners are registered in `ngAfterViewInit` and cleaned up in `ngOnDestroy`. Form controls implement `ControlValueAccessor` for `[(ngModel)]` and reactive forms support.

## Scripts

```bash
npm run dev       # ng serve (dev server)
npm run build     # ng build (production build)
npm run lint      # ng lint (Angular ESLint)
npm run preview   # ng serve --configuration=production
```
